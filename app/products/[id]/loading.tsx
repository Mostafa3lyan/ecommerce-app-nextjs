import React from 'react';
import LoadingComponent from '../../loading';
const Loading = () => {
    return (
        <div>
            <LoadingComponent />
        </div>
    );
}

export default Loading;
